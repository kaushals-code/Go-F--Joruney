namespace calculator

module Say =
    let hello name =
        printfn "Hello %s" name
    

module calci = 
    let add a b = a + b
    let subtract a b = a - b
    let multiply a b = a * b
